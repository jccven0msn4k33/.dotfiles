# This config assumes that you are using EdDSA signature over RSA one
# https://en.wikipedia.org/wiki/EdDSA
# Please use either Github CLI or the command below to generate one:
# ssh-keygen -t ed25519 -C "CustomUsername CustomTitle"

Host *
   PreferredAuthentications publickey
   IdentityFile ~/.ssh/id_ed25519
   AddKeysToAgent yes

Host ssh.dev.azure.com
   User git
   PreferredAuthentications publickey
   IdentityFile ~/.ssh/id_rsa
   HostKeyAlgorithms +ssh-rsa
   AddKeysToAgent yes

# Ensure that you have created a public key named id_work and id_personal
# and have added the corresponding public keys to your Github accounts

# Sample remote usage: git@github-personal:jcchikikomori/repo.git
Host github-personal
   HostName github.com
   User jcchikikomori
   PreferredAuthentications publickey
   IdentityFile ~/.ssh/id_personal
   AddKeysToAgent yes

# Sample remote usage: git@github-work:jcchikikomori/repo.git
# Host github-work
#    HostName github.com
#    User jccven0msn4k33
#    PreferredAuthentications publickey
#    IdentityFile ~/.ssh/id_work
#    AddKeysToAgent yes

Host github-rdy
   HostName github.com
   User jccven0msn4k33
   PreferredAuthentications publickey
   IdentityFile ~/.ssh/id_ed25519
   AddKeysToAgent yes
